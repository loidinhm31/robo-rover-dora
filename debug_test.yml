nodes:
  - id: test_keyboard
    build: cargo build --release
    path: target/release/arm_bot_controller
    args: local-keyboard
    inputs:
      tick: dora/timer/millis/100  # This is crucial!
    outputs:
      - local_keyboard
